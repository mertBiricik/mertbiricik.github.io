---
layout: base
title: Portfolio
description: Explore Mert Biricik's portfolio of full stack development, data science, and system engineering projects, including demos and data visualizations.
priority: 0.9
changefreq: monthly

---

<section class="section">
  <div class="section-container">
    <h1 class="section-title">Portfolio</h1>
    <p class="section-subtitle">Showcase: full stack development, data science, system engineering projects. Includes demos, data visualizations.</p>

    <div class="projects-grid">
      {%- for project in projects.all | reverse -%}
      <div class="project-card">
        <div class="project-header">
          <h3 class="project-title">{{ project.title }}</h3>
          {% if project.subtitle %}
          <p class="project-subtitle">{{ project.subtitle }}</p>
          {% endif %}
        </div>
        <div class="project-content">
          <div class="project-description">
            <p><strong>Problem:</strong> {{ project.problem }}</p>
            <p><strong>Solution:</strong> {{ project.solution }}</p>
            <p><strong>Outcome:</strong> {{ project.outcome }}</p>
          </div>
          {% if project.tech and project.tech.length > 0 %}
          <div class="project-tech">
            {%- for tag in project.tech -%}
              <span class="tech-tag">{{ tag }}</span>
            {%- endfor -%}
          </div>
          {% endif %}
          <div class="project-links">
            {% if project.sourceLink %}
              <a href="{{ project.sourceLink }}" class="btn btn-outline" target="_blank" rel="noopener noreferrer" style="margin-right: 10px;"><i class="fab fa-github"></i> View Code</a>
            {% endif %}
            {% if project.visitSiteLink %}
              <a href="{{ project.visitSiteLink }}" class="btn btn-primary" target="_blank" rel="noopener noreferrer" style="margin-right: 10px;">
                <i class="fas fa-external-link-alt"></i> Visit Site
              </a>
            {% endif %}
            {% if project.id == 'enerdata' %}
              <a href="/demos/enerdata-primary-energy/" class="btn btn-primary">
                <i class="fas fa-chart-bar"></i> View Demo
              </a>
            {% endif %}
          </div>
        </div>
      </div>
      {%- endfor -%}
    </div>

    <div class="text-center" style="margin-top: 3rem;">
      <h2>Collaboration?</h2>
      <p>Seeking exciting projects, innovative teams. Discuss collaboration.</p>
      <div class="hero-cta">
        <a href="/contact/" class="btn btn-primary">
          <i class="fas fa-envelope"></i>
          Contact
        </a>
        <a href="https://github.com/mertbiricik" class="btn btn-outline" target="_blank" rel="noopener noreferrer">
          <i class="fab fa-github"></i>
          See my GitHub
        </a>
      </div>
    </div>
  </div>
</section>